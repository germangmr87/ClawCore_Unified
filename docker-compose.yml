services:
  clawcore-gateway:
    image: ${CLAWCORE_IMAGE:-clawcore:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      CLAWCORE_GATEWAY_TOKEN: ${CLAWCORE_GATEWAY_TOKEN}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
    volumes:
      - ${CLAWCORE_CONFIG_DIR}:/home/node/.clawcore
      - ${CLAWCORE_WORKSPACE_DIR}:/home/node/.clawcore/workspace
    ports:
      - "${CLAWCORE_GATEWAY_PORT:-18789}:18789"
      - "${CLAWCORE_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${CLAWCORE_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]

  clawcore-cli:
    image: ${CLAWCORE_IMAGE:-clawcore:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      CLAWCORE_GATEWAY_TOKEN: ${CLAWCORE_GATEWAY_TOKEN}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
    volumes:
      - ${CLAWCORE_CONFIG_DIR}:/home/node/.clawcore
      - ${CLAWCORE_WORKSPACE_DIR}:/home/node/.clawcore/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
