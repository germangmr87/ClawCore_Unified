---
summary: "CLI reference for `clawcore uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `clawcore uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
clawcore uninstall
clawcore uninstall --all --yes
clawcore uninstall --dry-run
```
